<RULE[project]>
- **Deep Architectural Validation**: Always think deeply, clearly, and rigorously before implementing features. Validate assumptions end-to-end (e.g., test actual execution of API routes, don't just rely on static builds passing).
- **Honesty in Implementation**: Never "fake" capabilities or success states in the UI. If a library or tool lacks functionality (e.g., `pdf-lib` lacking AES encryption or OCR capabilities), explicitly state this limitation and block the implementation rather than building a dishonest UI stub.
</RULE[project]>
